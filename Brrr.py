class Carro:
    def __init__(self):
        super().__init__()

    def transformar(self) -> str:
        super().transformar()
        self.texto: str = ""
        if type == 1:
            self.texto = text.upper()
            return texto

        if type == 2:
            self.texto = text.lower()
            return texto

        if type == 3:
            self.texto = text.
            return texto